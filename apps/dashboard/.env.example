# =============================================================================
# REQUIRED: These must be set for the application to work
# =============================================================================

# Authentication (NextAuth + Google OAuth for dashboard login)
NEXTAUTH_SECRET=your-random-secret-here-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000
AUTH_GOOGLE_ID=xxxxxxxxxx.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=GOCSPX-xxxxxxxxxx
AUTHORIZED_EMAILS=your-email@gmail.com,another-email@gmail.com

# Redis - Required for sync state and metrics
# Auto-configured via Vercel Marketplace (Upstash integration)
# For local dev, connect to Vercel project or use a test Redis instance
UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxx

# =============================================================================
# OPTIONAL: Settings encryption (recommended for production)
# =============================================================================

# Encryption key for storing credentials in Redis (32-byte, base64 encoded)
# Generate with: openssl rand -base64 32
SETTINGS_ENCRYPTION_KEY=

# =============================================================================
# OPTIONAL: Notion Configuration (can be configured via web UI instead)
# =============================================================================

NOTION_API_TOKEN=secret_xxxxxxxxxxxxx
NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# OPTIONAL: Google Calendar Configuration (can be configured via web UI instead)
# =============================================================================

GOOGLE_CALENDAR_CLIENT_ID=xxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CALENDAR_CLIENT_SECRET=GOCSPX-xxxxxxxxxx
GOOGLE_CALENDAR_REFRESH_TOKEN=1//xxxxxxxxxx
GOOGLE_CALENDAR_CALENDAR_ID=primary

# =============================================================================
# OPTIONAL: Webhooks & Admin
# =============================================================================

WEBHOOK_URL=https://your-domain.vercel.app/api/webhooks/google-calendar
ADMIN_SECRET=your-admin-secret
CRON_SECRET=your-cron-secret

# =============================================================================
# OPTIONAL: Development settings
# =============================================================================

NODE_ENV=development
LOG_LEVEL=info
